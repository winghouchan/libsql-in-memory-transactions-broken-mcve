# libsql-in-memory-transactions-broken-mcve

A minimal, complete, verifiable example of how transactions are broken in libSQL. See the [test file](./index.test.js).

## Setup

```bash
npm install
```

## Reproduction

```bash
node --test
```
